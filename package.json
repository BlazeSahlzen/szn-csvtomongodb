{
  "name": "CSV-JSON-to-MongoDB",
  "version": "1.0.0",
  "description": "Converting complex CSV files to JSON using csv-to-json npm package and then converting the JSON records into specified MongoDB schema forms before insertion or updation.",
  "main": "index.js",
  "dependencies": {
    "csvtojson": "^0.5.3",
    "uuid": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^1.9.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "watch": "nodemon index.js"
  },
  "author": "Soubhik Mondal <souvikmondal.j@gmail.com>",
  "license": "ISC"
}
